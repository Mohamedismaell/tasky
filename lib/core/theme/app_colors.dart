import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();
  // Brand
  static const primaryDark = Color(0xFF15B86C);
  static const primaryLight = Color(0xFF14A662);

  // Backgrounds
  static const backgroundDark = Color(0xFF181818);
  static const backgroundLight = Color(0xFFF6F7F9);

  // Surfaces
  static const surfaceDark = Color(0xFF282828);
  static const surfaceLight = Color(0xFFFFFFFF);

  // Text
  static const textPrimaryDark = Color(0xFFFFFCFC);
  static const textPrimaryLight = Color(0xFF161F1B);

  static const textSecondaryDark = Color(0xFFC6C6C6);
  static const textSecondaryLight = Color(0xFF3A4640);

  static const placeholderDark = Color(0xFF6D6D6D);
  static const placeholderLight = Color(0xFF9E9E9E);

  // Outline
  static const outlineDark = Color(0xFF6E6E6E);
  static const outlineLight = Color(0xFFD1DAD6);
}
